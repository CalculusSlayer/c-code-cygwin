#include <stdlib.h>
#include <stdio.h>

// Execute the "ls" command, which lists the files in the current directory
int main(void) {

    char *s = NULL;
    //printf("type of system function return value: %s\n", typeof('ff')); // doesn't work
    system("touch a.txt");
    return 0;

  return 0;
}
